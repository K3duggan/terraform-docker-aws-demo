# ── Terraform local state & caches (sensitive) ─────────────────────────
*.tfstate
*.tfstate.*
.terraform/          # provider plugin cache, etc.
crash.log
crash.*.log

# If you keep a local state file named like this
.terraform.tfstate

# ── Local variables (often contain secrets) ────────────────────────────
terraform.tfvars
*.tfvars
*.auto.tfvars
*.tfvars.json

# ── Local overrides (developer-specific) ───────────────────────────────
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ── Credentials / keys / env files (very sensitive) ────────────────────
*.pem
*.key
id_rsa*
id_ed25519*
*.pfx
*.p12
.env
.env.*
*.credentials
*.keystore

# ── OS/editor clutter ──────────────────────────────────────────────────
.DS_Store
*.swp
